{
  "scripts": {
    "start": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "concurrently": "^9.0.0"
  }
}